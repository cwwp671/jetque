"""
File: src.animations.statics.stationary_animation.py

    Class: StationaryAnimation(StaticAnimation)
		Attributes:
			# Possibly no unique attributes, but we want this file / class anyways
        Methods:
			__init__(parameters supplied from config / AnimationFactory)
				# Super the Parent Init
                # Initializes any stationary specific attributes
            play()
                # Plays the animations involved
            stop()
                # Stops the animations involved
            setup_animations()
                # Sets up any necessary stationary animation settings and groups
            connect_signals()
				# Supers the Parent
                # Connects any necessary stationary animation signals to handlers
			connect_slots()
				# Supers the Parent
				# Connects any necessary stationary animation slots to handlers

"""
