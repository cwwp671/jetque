"""
File: src.animations.dynamics.directional_animation.py

    Class: DirectionalAnimation(DynamicAnimation)
		Attributes:
			# Possibly no unique attributes, but we want this file / class anyways
        Methods:
			__init__(parameters supplied from config / AnimationFactory)
				# Super the Parent Init
                # Initializes any directional specific attributes
            play()
                # Plays the animations involved
            stop()
                # Stops the animations involved
            setup_animations()
                # Sets up any necessary directional animation settings and groups
            connect_signals()
				# Supers the Parent
                # Connects any necessary directional animation signals to handlers
			connect_slots()
				# Supers the Parent
				# Connects any necessary directional animation slots to handlers
"""
